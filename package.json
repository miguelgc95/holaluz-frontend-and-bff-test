{
	"name": "holaluz-frontend-and-bff-test",
	"version": "0.1.0",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"frontend": "yarn workspace frontend run serve",
		"backend": "yarn workspace backend run start",
		"build:frontend": "yarn workspace frontend run build",
		"lint:frontend": "yarn workspace frontend run lint"
	}
}
